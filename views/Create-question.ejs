<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/style.css" rel="stylesheet">
    <title>YOUR QUESTIONS </title>
</head>
<body>
    <div>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
           <% for(var i=0; i<messages.error.length; i++) { %>
                   <li class="text-gray rounded px-4 py-4 bg-yellow-500"> <%= messages.error[i] %></li>
           <% } %>
         <% } %>
      </div>
    
    <div class="relative flex min-h-screen flex-col justify-center overflow-hidden py-6 bg-gray ">
        <div class="relative bg-gray px-6 pb-5 shadow-xl ring-1 ring-white-900/5 sm:mx-auto sm:max-w-lg sm:rounded-lg sm:px-10">
            <div class="mx-auto max-w-md">
                <h2 class="text-2xl  text-center font-bold my-5"> Add Question From Here
                    
                </h2>
               
                <div class="divide-y  divide-gray-300/50">
                    <div class="space-y-6 text-base leading-7 text-ray-600">
                        <form action="/Election/<%=id%>/NewQuestion/Create" method="post">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <label for="quest" class="text-md w-40 inline-block font-medium leading-5 text-gray-700">
                               Question
                            </label>
                            <input required type="text" id="quest" name="question" class="bg-gray-100 inline-block rounded-lg shadow-md py-3 px-4 mb-5 text-base leading-6 placeolder-gray-500" placeholder="Enter Question" required><br/>
                            <label for="descrip" class="text-md w-40 inline-block font-medium leading-3 text-gray-700">Description</label>
                            <input required type="text" id="descrip" name="description" class="bg-gray-100 inline-block rounded-lg shadow-md py-3 px-4 mb-5 text-base leading-6 placeolder-gray-500" placeholder="Enter Brief Description"><br/>
                            <button type="submit" id="submit" class="w-fit rounded-lg shadow-lg px-5 py-3 bg-violet-500 text-white hover:bg-green-400 focus:outline-none focus block">Submit</button>
                        </form>
                    </div>
                     </div>
            </div>
        </div>
    </div>
</body>
</html>
